// Test variable binding with let
cntr [
  0 |> $

  # |^ val => {
    let doubled = val * 2
    if val < 5 {
      IO.puts(doubled)
      val + 1 |> $
    } else {
      halt
    }
  }
]

main:
cntr	-->v
^<--	<--v
